{
  "name": "bug-training-game-frontend",
  "version": "1.0.0",
  "description": "Frontend for Bug Training Game",
  "scripts": {
    "dev": "http-server -p 8080 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "proxy": "http://localhost:3000"
} 